import { EstiloEnfasisDirective } from './estilo-enfasis.directive';

describe('EstiloEnfasisDirective', () => {
  it('should create an instance', () => {
    const directive = new EstiloEnfasisDirective();
    expect(directive).toBeTruthy();
  });
});
